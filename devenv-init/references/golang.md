# Go templates (planned)

Not yet implemented. Tracked for: `golang` (bare).

When added, expected toggles:
- `features.golangci-lint.enable`
- `features.air.enable` (live-reload)

Until then, hand-write devenv.nix using `languages.go.enable` from devenv docs.
